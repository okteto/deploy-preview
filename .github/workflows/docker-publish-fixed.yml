name: Build and publish Docker image to GHCR (fixed)

on:
  push:
    branches: [ feature/provma-preview, main ]

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: set-image-owner
        name: Set image owner (lowercase)
        run: |
          owner=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')
          echo "owner=$owner" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ___PROVMAR_01_
          file: ___PROVMAR_01_/Dockerfile.preview
          push: true
          tags: |
            ghcr.io/${{ steps.set-image-owner.outputs.owner }}/provma-preview:latest
            ghcr.io/${{ steps.set-image-owner.outputs.owner }}/provma-preview:${{ github.sha }}
